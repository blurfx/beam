<UserControl x:Class="Beam.View.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Beam.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListView x:Name="lTweet" SelectionMode="Single" ScrollViewer.CanContentScroll="False" ScrollViewer.HorizontalScrollBarVisibility="Hidden" BorderBrush="{x:Null}"  >
        <ListView.ContextMenu>
            <ContextMenu>
                <MenuItem Header="답장(_R)" />
                <MenuItem Header="리트윗(_T)" />
                <MenuItem Header="관심글(_F)" />
            </ContextMenu>
        </ListView.ContextMenu>
    </ListView>
</UserControl>
